services:
  gitlab-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gitlab-cicd-dashboard
    ports:
      - "3000:3000"
    environment:
      - GITLAB_URL=https://gitlab.com
      - GITLAB_TOKEN=glpat-K1vwJmt446_VqfWreWFFV286MQp1OjhnZGxtCw.01.121n75iit
      - NEXT_PUBLIC_GITLAB_URL=https://gitlab.com
      - NEXT_PUBLIC_GITLAB_TOKEN=glpat-K1vwJmt446_VqfWreWFFV286MQp1OjhnZGxtCw.01.121n75iit
    restart: unless-stopped
    networks:
      - gitlab-network

networks:
  gitlab-network:
    driver: bridge
