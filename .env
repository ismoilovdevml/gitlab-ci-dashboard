# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres 
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the 
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

# Database
DATABASE_URL="postgresql://gitlab_dashboard:gitlab_dashboard_2024@localhost:5432/gitlab_dashboard?schema=public"

# Redis
REDIS_URL="redis://:redis_2024@localhost:6379"

# App
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# GitLab (optional - can be configured in UI)
NEXT_PUBLIC_GITLAB_URL="https://gitlab.com"
NEXT_PUBLIC_GITLAB_TOKEN=""